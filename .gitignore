# Python env / caches / packaging
.venv/
__pycache__/
*.pyc
*.egg-info/

# Data (regenerable — see README/docs/DATASETS.md)
ml-1m/
ml-1m.zip
datasets/
raw/
sweep.log
nohup.out

# TensorBoard artifacts — regenerable via tb_export.py
runs/
runs_archive/

# Best-model checkpoints (train_*.py) — regenerable
checkpoints/
