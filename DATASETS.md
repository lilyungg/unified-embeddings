# DATASETS.md — обработка данных и нюансы обучения по датасетам

Отдельная страница, чтобы не смешивать датасеты: для каждого — источник,
препроцессинг, сплит, словарь, маппинг бюджетов на колонки статьи и особенности
обучения. Общий контекст статьи и результаты — в README.

Статья наследует препроцессинг: MovieLens и Criteo — из Wang et al. 2021
(DCN-V2), Avazu — из Song et al. 2019 (AutoInt). Это первоисточники истины,
текст самой UE-статьи местами неточен (см. ловушку MovieLens).

---

## MovieLens-1M — ✅ воспроизведено (2026-07-12)

### Данные
- Источник: `ml-1m.zip` с grouplens.org (24MB, качается однократно).
  Loader: `load_movielens` в data.py — читает `ratings.dat` + `users.dat`.
- Признаки (6, все категориальные): `user_id`, `movie_id`, `gender`, `age`,
  `occupation`, `zip`. `age` в ML-1M — уже бакеты {1,18,25,35,45,50,56},
  continuous-фич нет. `movies.dat` (жанры/названия) не используется — как в статье.

### Метка — ГЛАВНАЯ ЛОВУШКА (стоила 3 п.п. AUC)
- `--ml-labels wang` (дефолт, реальный пайплайн статьи через DCN-V2):
  рейтинги 1–2 → 0, 4–5 → 1, **тройки удаляются**. Остаётся ~740K примеров
  (~78% позитивов) — совпадает с Table 3 в DCN-V2.
- `--ml-labels ge3` (буквальный текст UE-статьи «rating ≥ 3 → 1»): 1M примеров,
  ~84% позитивов. Задача сложнее (тройки — неразделимые позитивы на границе
  классов), все ячейки садятся ≈ на 3 п.п. ниже статьи. Использовать только
  чтобы показать эффект ловушки.

### Сплит
- Random 80/10/10 по примерам, seed 42 (`random_split` в data.py), **после**
  фильтрации троек: ~592K / 74K / 74K. Совпадает с протоколом статьи (DCN-V2:
  «randomly split into 80/10/10»). Один юзер может быть и в train, и в test —
  стандартная CTR-постановка, у авторов так же.
- Сид сплита фиксирован во всех прогонах; `--runs N` меняет только инициализацию
  и порядок батчей (поэтому std маленький, ±0.0002–0.0005).

### Словарь и бюджеты
- Без прунинга, строится по факту из данных: total_vocab = 13,177 при wang
  (13,215 при ge3 — 38 редких значений живут только в строках с тройками).
- База бюджета `emb_levels` = 13,653 ≈ полный collisionless-бюджет.
  Маппинг на колонки Table 1: 1.0× = 1.64MB ↔ «1.6MB», 0.5× ↔ «791kB»,
  0.1× ↔ «158kB».
- Отличие от статьи: gender у нас в общей таблице; в статье gender всегда
  collisionless (влияет только на multiplex-строки, эффект мал).

### Обучение (подобранный конфиг зашит в дефолты — флаги не нужны)
```bash
.venv/bin/python run.py --ml1m ml-1m --skip avazu criteo --budgets 1.0 0.5 0.1
```
- Архитектура (из DATASET_CFG, по статье): 1 cross + DNN 192, emb_dim 30.
- batch 128 — как в статье для ML (лучше 512); lr 1e-3 — лучше статейного 2e-4
  в нашем сетапе; **BatchNorm и dropout выключить** (в статье их нет, с ними
  −0.5–1 п.п.); init эмбеддингов uniform(±0.05) — критично (зашито в ue.py).
- Переобучение начинается с ~6–8 эпохи; early stopping patience 5 достаточно.
  На MPS ~15 с/эпоха, полный свип 3 бюджета × 6 экспериментов ≈ 30 мин.
- wd=0 не меняет; глубже сеть (2 cross, DNN 256/128) — хуже.

---

## Avazu — ✅ воспроизведено (2026-07-14, A100, single run)

Итог (наш / статья, DCN): 32.4MB 0.7765/0.7735 · 3.24MB Multiplex 0.7737/0.7718,
Non-multiplex 0.7679/0.7671 · 324kB Multiplex 0.7702/0.7686, Non-multiplex
0.7525/0.7510. Разрыв Multiplex−Non-multiplex +0.13/+0.58/+1.77 п.п. (статья
+0.11/+0.47/+1.76). Полная таблица — в README. Сырьё нашлось в criteo-тарболе
(`raw/train.gz`), Kaggle не понадобился.


### Подготовка данных (однократно, на машине с данными)
```bash
# вход: сырой Kaggle train.gz (или распакованный train.csv)
python prepare_data.py avazu --raw train.gz   # -> datasets/avazu_prepared.parquet + .vocab.json
python run.py --ml1m ml-1m --skip movielens criteo   # подхватит prepared автоматически
# или явно: --avazu path/to/avazu_prepared.parquet
```
`prepare_data.py` (двухпроходный, стриминг через polars lazy — полный датасет
в RAM не материализуется): (1) частоты значений → пруниг словаря до Table 5
статьи (total 252,838; редкие → OOV-бакет 0, топ-K−1 → коды 1..K−1 по убыванию
частоты); (2) кодирование в int32 → parquet. `hour` YYMMDDHH → час суток
(24 значения, «mod 24» статьи); `id` выброшен. .gz распаковывается во временный
файл (scan_csv не стримит gzip).

### Метка
- `click` 0/1 нативная — ловушки нет.

### Сплит
- Prepared-путь: shuffle, 10% test (как AutoInt/статья), val выкраивается из
  остатка → 81/9/10 (у статьи val не описан, test совпадает).

### Закрытые ловушки (были TODO)
1. ~~Пруниг словаря~~ — prepare_data.py, Table 5 зашита в `AVAZU_VOCAB`.
2. ~~hour mod 24~~ — prepare_data.py.
3. ~~Сплит 90/10~~ — prepared-ветка `load_avazu`.
- Легаси-ветка reczoo x4 (без прунинга, сплит 80/10/10) осталась как fallback,
  когда prepared-файла нет — для сравнения со статьёй её НЕ использовать.

### Словарь и бюджеты
- База `emb_levels` = 26,542 = 3.24 MiB — это **средняя** колонка статьи
  «3.24MB» (0.1× от collisionless). Маппинг: 1.0× ↔ 3.24MB, 0.1× ↔ 324kB;
  колонка 32.4MB = budget 10.0 (collisionless и так гоняется отдельно).

### Обучение (сетап статьи, наш batch/lr не тюнился)
- Архитектура: 1 cross + DNN 512×2, emb_dim 32; batch 512, lr 2e-4, 300K шагов.
- Переносимо с ML: init uniform(±0.05), без BN/dropout. batch/lr тюнить заново.

---

## Criteo — ✅ воспроизведено (2026-07-14, A100, single run)

Итог (наш / статья, DCN): 25MB 0.8095/0.8070 · 12.5MB Multiplex 0.8090/0.8047,
Non-multiplex 0.8058/0.7998 · 2.5MB Multiplex 0.8082/0.8049, Non-multiplex
0.7989/0.7944. Multiplex почти не проседает при 10× сжатии (−0.10 п.п.), тогда
как Non-multiplex теряет 0.78 — главный тезис статьи. Continuous I1–I13 в модели
(dense-путь), словарь пруненный до Table 4. Полная таблица — в README.


### Подготовка данных (однократно, на машине с данными)
```bash
# вход: сырой Kaggle train.txt (Display Advertising Challenge, tab-separated)
python prepare_data.py criteo --raw train.txt   # -> datasets/criteo_prepared.parquet + .vocab.json
python run.py --ml1m ml-1m --skip movielens avazu    # подхватит prepared автоматически
# или явно: --criteo path/to/criteo_prepared.parquet
```
`prepare_data.py`: пруниг словаря до Table 4 статьи (total 160,605, зашито в
`CRITEO_VOCAB`); continuous I1–I13 нормализуются как в DCN-V2 — log(x+4) для I2,
log(x+1) для остальных (аргумент клипается снизу единицей, пропуски → 0);
категории → int32-коды; **порядок строк сохраняется** (нужен темпоральному
сплиту). Выход: label + I1–I13 (float32) + C1–C26 (int32).

### Метка
- `Label` 0/1 нативная — ловушки нет.

### Сплит
- Prepared-путь: темпоральный как в статье — первые 6/7 строк train, последняя
  1/7 случайно 50/50 val/test (`temporal_split` в data.py). В Kaggle-файле нет
  колонки дня, но строки хронологические — границы дней аппроксимируются долей
  строк. `--fast` берёт **головной** срез (не random), чтобы не ломать хронологию.

### Dense-путь в модели
- `load_criteo` возвращает dense-матрицу; она конкатенируется к эмбеддингам
  перед cross-слоем (x0 в DCN-V2 = [эмбеддинги; continuous]) — models.py
  `forward(x, dense)`, размер входа сети = n_cat × emb_dim + 13.

### Закрытые ловушки (были TODO)
1. ~~Continuous выброшены~~ — prepare_data.py + dense-путь в models/train/benchmark.
2. ~~Пруниг словаря~~ — prepare_data.py, Table 4.
3. ~~Темпоральный сплит~~ — prepared-ветка `load_criteo` (аппроксимация по строкам).
- Легаси-ветка reczoo x4 (только категории, random-сплит) осталась как fallback —
  для сравнения со статьёй её НЕ использовать.

### Словарь и бюджеты
- База `emb_levels` = 83,886 = 12.5 MiB — **средняя** колонка «12.5MB» (0.5×).
  Маппинг: 1.0× ↔ 12.5MB, 2.0× ↔ 25MB, 0.2× ↔ 2.5MB.

### Обучение (сетап статьи)
- Архитектура: 2 cross + DNN 748×2, emb_dim 39; batch 512, lr 2e-4, 300K шагов.
- Переносимо с ML: init uniform(±0.05), без BN/dropout. batch/lr тюнить заново.

---

## Общие правила (все датасеты)

- **Prepared-файлы (int-коды) хэшируются быстрым путём**: для integer-колонок
  prehash/prehash_split используют векторный splitmix64 с солью feature-id
  вместо строкового xxhash (иначе 45M×26 значений — часы). preencode для
  int-кодов — просто сдвиг +1 (0 зарезервирован под OOV), build_vocabs размером
  покрывает весь диапазон кодов (важно при `--fast`-сэмпле). Строковый путь
  (MovieLens, легаси) не изменён — регрессия проверена бит-в-бит.
- Prepared parquet в RAM ~7GB для Criteo (int32 вместо строк) — полный прогон,
  вероятно, влезает и в 24GB, но prehash/обучение на 45M строк всё равно
  разумнее на большой машине.

- Init эмбеддингов uniform(±0.05) зашит в ue.py — не возвращать дефолт PyTorch
  N(0,1), он стоит ~5 п.п. из-за гигантских стартовых норм.
- Collisionless не зависит от бюджета — гоняется один раз при максимальном
  из `--budgets`.
- 5 прогонов (`--runs 5`) — протокол статьи (mean±std, Welch's t-test);
  `--paper-protocol --patience 0` — оценка test каждую эпоху + best-over-epochs
  (протокол Tsang & Ahle, различие с нашим val-отбором ≈ +0.1 п.п.).
- Diff метрик делать только с той колонкой Table 1, на которую замаплен бюджет
  (см. маппинги выше), и только для DCN-строк (MLP-ветки в статье нет).
